/**
 * useChat — Custom Hook (placeholder)
 *
 * Destino: encapsular toda la lógica de Supabase Realtime para el chat médico-paciente.
 * Actualmente la lógica vive en ChatMedico.jsx.
 * Se migrará aquí en la siguiente fase de refactorización.
 *
 * Uso futuro:
 *   const { mensajes, enviando, handleEnviar, fetchMensajes } = useChat(doctorId, pacienteId);
 */
export function useChat(doctorId, pacienteId) {
  // TODO: Migrar lógica de ChatMedico.jsx aquí (fetchMensajes + Realtime channel)
  throw new Error('useChat aún no está implementado. Usa la lógica local por ahora.');
}
