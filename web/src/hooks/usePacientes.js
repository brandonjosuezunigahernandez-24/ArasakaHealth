/**
 * usePacientes — Custom Hook (placeholder)
 *
 * Destino: encapsular toda la lógica de Supabase relacionada con pacientes.
 * Actualmente la lógica vive directamente en PacientesList.jsx e HistorialClinico.jsx.
 * Se migrará aquí en la siguiente fase de refactorización.
 *
 * Uso futuro:
 *   const { pacientes, loading, fetchPacientes } = usePacientes(doctorId);
 */
export function usePacientes(doctorId) {
  // TODO: Migrar lógica de PacientesList.jsx y HistorialClinico.jsx aquí
  throw new Error('usePacientes aún no está implementado. Usa la lógica local por ahora.');
}
