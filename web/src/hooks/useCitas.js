/**
 * useCitas — Custom Hook (placeholder)
 *
 * Destino: encapsular toda la lógica de Supabase relacionada con citas.
 * Actualmente la lógica vive en CitasCalendario.jsx.
 * Se migrará aquí en la siguiente fase de refactorización.
 *
 * Uso futuro:
 *   const { eventos, fetchCitas, handleCitaAgendada, handleCitaActualizada, handleCitaEliminada } = useCitas(doctorId);
 */
export function useCitas(doctorId) {
  // TODO: Migrar lógica de CitasCalendario.jsx aquí
  throw new Error('useCitas aún no está implementado. Usa la lógica local por ahora.');
}
